<%= render 'form', project: @project, comment: @comment %>

<%= link_to("Delete", project_comment_path(@project.id, @comment.id), method: :delete, data: {confirm: "Are you sure you want to delete this comment? This cannot be undone!"}) %>
